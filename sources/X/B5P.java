package X;

import android.graphics.Canvas;
import android.graphics.Matrix;
import android.graphics.RectF;

public interface B5P extends B0T {
    void B5x(Canvas canvas, Matrix matrix, int i);

    void B8v(Matrix matrix, RectF rectF, boolean z);
}
